# PVG–ANT Central Mind Skill Stack

سبعُ طبقاتٍ مرتّبة. الحوكمةُ تعلو الإبداع.

```text
1. Logic Layer
Purpose: define statement / proof / model / certificate.
Main source: Mileti (planned, v0.2).

2. ANT Layer
Purpose: convert arithmetic phenomena into analytic objects.
Main sources: Overholt (retrofitted), Tenenbaum (through 004-Z).

3. PVG Layer
Purpose: read arithmetic through nu(n): support, height, phase,
         divisor box, residue fibers, tail, edge.

4. Sieve Layer
Purpose: classify sieve information and walls (parity, Type I/II, level).

5. Spectral Layer
Purpose: prevent unsupported operator claims; require spectral certificate.

6. Computational Layer
Purpose: experiments and reproducible diagnostics, not proofs.

7. Governance Layer
Purpose: bibliographic verification, certificate ledger,
         forbidden claims, release policy.  (HIGHEST authority)
```

## قاعدة السلطة

عند التعارض بين طبقةٍ إبداعيّة (polymath/ANT/PVG) وطبقة الحوكمة (certificate-ledger/release-governance)، **تنتصر الحوكمة**.

**Related registry IDs:** جميع `SKILL-*` في `01-registries/skills.jsonl`.
**Honest classification:** Diagnostic (architecture map). No RH/GRH progress.
