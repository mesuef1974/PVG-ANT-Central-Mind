# v0.7-A Closure Review — OPERA-004-A (Sifting Sequences as Main-Term/Remainder Certificates)

Registry ID: `AUDIT-CM-V07-A-CLOSURE-001` · Book: `BOOK-SIEVE-OPERA-001` · Unit: `OPERA-004-A` · **HEAD reviewed: `a319df4`**.

## Result: RECOMMEND close → OPERA-004-A CLOSED

The unit is source-grounded, ceiling-clean, and internally coherent at the reviewed commit. Closure is executed in a separate second commit; the book stays `partial_overlay` (one closed unit), Montgomery MNT-II untouched.

## Review trail (blockers were structural state contradictions, never mathematics)

```text
9fdb009  Open Opera de Cribro scope-only
7fc4435  Add Opera sifting-sequence certificate foundation      (intake A, validated_intake)
1572da0  Correct Opera v0.7-A intake routing and boundaries     (Attempt 1 = RETURN fix — but LEFT
                                                                  INTERNALLY INCOMPLETE: it fixed body §9
                                                                  yet left a stale packet_mismatch=false in
                                                                  the unit HEADER; header vs body contradicted)
a319df4  Fix residual packet_mismatch header in Opera OPERA-004-A intake  (consistency fixup; header/§9/§13
                                                                  + all three ledgers now uniform)
```

`1572da0` is kept visible in the trail and described as internally incomplete, then completed by `a319df4` — not hidden or narratively merged. This is an INTAKE correction, not a State-Repair: no closed unit and no trusted claim was altered (the unit was never closed).

## Adversarial reviews (three independent falsifiers)

```text
Attempt 1 = BLOCK — packet_mismatch recorded false; must be true / routing_split.
Attempt 2 = BLOCK — the named correction commit 1572da0 was self-contradictory (committed header=false
                    vs committed body=true); the header fix existed only as an uncommitted working-tree
                    edit. A committed closure must reference a coherent committed state.
Attempt 3 = SAFE TO CLOSE — on committed state a319df4: eight-step check all PASS; both prior blockers
                    genuinely fixed in committed history; no remaining defect.
```

## The four contested points — adjudicated from literal text

1. **packet_mismatch = true, mismatch_type = routing_split.** Frozen hypothesis (`v0.7-scope.md` "Early: inclusion-exclusion, Brun, Selberg" row, verbatim): primary `reference_integration` ("duplicate-pressure vs Harman / Montgomery D", provisional). The packet grounded primary `core_certificate_theory` with `reference_integration` demoted to secondary, and minted new organizing content. The PRIMARY axis changed ⇒ `routing_split` (not refinement — refinement requires the primary class unchanged). Original hypothesis preserved verbatim, demoted not erased; the retroactive "refinement, not contradiction / packet_mismatch = false" phrasing removed.

2. **Seven cards `TREASURE-OPERA-001..007` = `provisional_intake` at review time.** Each carries `source_grounded=true`, `duplicate_status` (001/002 `integration_only`; 003-007 `new_function`), `scientific_class`, `trusted_status=provisional_intake`. On this closure they are promoted to `closure_approved` — not before.

3. **Three boundaries — ZERO new WALL-IDs, zero silent widening** (exact semantic comparison against the registered wall texts):
   - `aggregate-remainder` → Boundary. `WALL-SIEVE-CEILING` text is literally "The sixth wall is empty: no unconditional sieve reaches RH; only EH crosses" — it does NOT cover aggregate remainder failure, so NO mapping (no silent widening).
   - `local-model` → Boundary + governance caution (repairable via LB-05 — the opposite of a wall).
   - `formulation-vs-certificate` → Boundary + governance caution. NOT mapped to `WALL-PARITY`: this packet has not reached the parity mechanism and the gap is more general than parity; parity re-examination is DEFERRED to the parity-grounding packet (a named, visible deferral). `walls.jsonl` holds no OPERA-* wall.

4. **Adversarial matrix — 7 rows, 7/7** (on a319df4): T0 clean baseline (PASS) · T1 illegal closure without review · T2 illegal OPERA-004-B · T3 missing packet grounding · T4 illegal status transition · T5 stale next-action pointer · T6 Montgomery partial_overlay regression — each caught, state restored. (The earlier "6/6" dropped the T0 baseline row; the correct count is 7/7.)

## Source grounding & ceiling

Every claim traces to the packet (`S(A,z)=Σ μ(d) A_d`; `A_d=g(d)X+r_d`; aggregate-remainder control; the `(A,g,X,{r_d})` law; model-subtraction `c_n=a_n-(A(x)/x)b_n`; Ch 18 reuse) or a standard identity (Möbius detector, Eratosthenes–Legendre). No new theorem: §12 declares New Theorem = none, Candidate Mechanism = none. Ceiling fenced throughout: sieve formulation != prime production; individual `r_d` control != aggregate control; `g` = local model, not global independence; no example is a twin/Goldbach/parity/MC-001 path; `WALL-PARITY` uncrossed; zero RH progress, zero GRH progress. Unit is `core_certificate_theory` (not `application_specific_certificate`), so no `C_sieve` record is required, and none is claimed.

## Guards

Seven guards PASS (honesty · registry_sync · no_pdf · forbidden_promotion · duplicate_concept · citation · state_coherence). No new TOOL-ID, no new WALL-ID. Montgomery MNT-II remains `book_overlay_closed` (AUDIT-CM-MONTGOMERY-OVERLAY-008), untouched.

## On closure (this round, one unit only)

Cards `provisional_intake` → `closure_approved`; unit Status → CLOSED; books.jsonl unit note → closed; Opera stays `partial_overlay`; `next-action.md` → NONE awaiting an independently authorized next packet; `planned.jsonl` emptied. No `OPERA-004-B`, no Chapter 2, no book-overlay closure, no `declared_objective_coverage.complete` flip, no mastery.

**Honest classification:** Governance / Closure Review (unit, coverage-level). No RH progress. No GRH progress. No mastery.
