# External Review Memo — PVG–ANT Central Mind / One-Theorem 001

**Audience:** external mathematical reviewers (priority specialist; proof referee).
**Length policy:** this memo is the whole ask; appendices exist for depth on demand.

## 1. What the project is

A governed research program that develops prime-valuation geometry (PVG) —
integers as labeled valuation vectors \(\nu(n)=(v_p(n))_p\) with divisor-box,
support, margin, residue-fiber, and loss-graded morphism structure — into a
working two-way language with analytic number theory, and uses it to seek one
original, correct, modest ANT contribution. Claims are gated: no certificate,
no theorem; no priority search plus referee, no originality.

## 2. Current mathematical state

One complete internal crossing of the research route exists. Starting from
margin-interior divisor-box geometry the program produced the observable

\[
I_r(n)=\prod_{p^{\alpha}\parallel n}\max(\alpha-2r+1,\,0),
\]

proved multiplicativity, the Bell series \(1+\dfrac{y^{2r}}{(1-y)^2}\), the
twisted factorization
\(D_{r,\chi}(s)=L(2rs,\chi^{2r})\,L((2r+1)s,\chi^{2r+1})^{2}H_{r,\chi}(s)\)
with \(H\) absolutely convergent for \(\Re(s)>\tfrac{1}{2r+2}\), and — for
fixed \(q\ge 1\), \(r\ge 1\), reduced class \((a,q)=1\), fixed smooth compactly
supported \(W\) — a smoothed asymptotic with explicit simple- and double-pole
constants and remainder \(O_{q,r,W,\varepsilon}(x^{1/(2r+2)+\varepsilon})\).

## 3. The result and its exact claim ceiling

```text
Internally proved fixed-parameter weighted ANT result.
Exact prior-art status unresolved.
PVG materially contributed to discovery and formulation.
PVG necessity in the final proof is weak or not established.
```

The analytic machinery is classical; quadratic/cubic torsion-character
selection is classical (2013–2014 squarefull-progression literature). No
originality claim is made. A pre-registered stopping protocol fixes the
decision tree for every review outcome before any packet is sent.

## 4. Capability measurement (context, not a claim)

Internal registry-coverage scores exist (Benchmark 001) and are explicitly
not model performance. A sealed two-set adversarial benchmark
(specification: Appendix B) is registered and NOT_STARTED; the raw Set-A
baseline will be the first autonomous-performance measurement, and the
single sealed Set-B run the only generalization measurement.

## 5. Questions for reviewers — answer each independently

1. Is the statement correct as written?
2. Is the proof correct?
3. Is the result known (priority / literature)?
4. Was the geometric layer material — separately for: observable selection;
   formula discovery; local-factor identification; proof steering;
   generalization proposal?
5. What is missing that would change any of the above answers?

## 6. Environment reliability (one line)

All state, claims, and records in this packet are produced under a machine-
enforced governance gate; reviewers who wish to audit the environment's
trustworthiness — that nothing here was selected after results — see
Appendix A.

Appendices: **A** governance reliability · **B** Benchmark 002 protocol ·
**C** the \(I_r\) file (statement, hypotheses, proof route, PVG role,
prior-art search state).

```text
zero RH progress · zero GRH progress · no secured path
```

**Classification:** External-review memo. No originality claim.
