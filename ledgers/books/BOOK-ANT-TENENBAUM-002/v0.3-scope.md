# v0.3 Scope — Tenenbaum-005-A: Dirichlet Characters as Residue-Fiber Observables

Book ID: `BOOK-ANT-TENENBAUM-002` · **Opening only: scope, no unit writing yet, no reading execution yet.**

## Purpose

توسيعُ محرّك المرصد الحسابيّ (Tenenbaum حتى 004-Z) إلى **الألياف الباقية**: قراءةُ شخصيّات Dirichlet `χ mod q` كمراصدِ طورٍ على المحاور التطابقيّة، وربطُها بمجاميع الشخصيّات و`L(s,χ)` وتوزيعِ الأوّليّات في المتتاليّات — **كمراصدَ وشهاداتٍ ناقصة، لا كنتائجَ جديدة.**

## In scope — v0.3 (one planned unit)

| Unit (planned) | Topic | Builds on (live) | Planned targets (registered on execution open) |
|---|---|---|---|
| Tenenbaum-005-A | Dirichlet characters as residue-fiber observables | `OBS-CHARACTER-001` · `TOOL-CHARACTER-ORTHOGONALITY-001` · `WALL-SIEGEL` | a residue-fiber observable diagnostic · a character-sum phase reading · an AP-distribution **certificate requirement** (not a proof) |

**Registration (execution opened):** المعرِّفاتُ سُجِّلت ورُقِّيت مع `Tenenbaum-005-A`: `OBS-RESIDUE-FIBER-001` (Reinterpretation) · `TOOL-CHARACTER-SUM-PHASE-001` (Known)؛ توزيعُ AP/GRH-level = شهادةٌ ناقصة `MC-005`. لم يُكرَّر `OBS-CHARACTER-001` القائم.

## Ceiling (نافذ)

- توزيعُ الأوّليّات في المتتاليّات = Siegel–Walfisz (Known، جدار `WALL-SIEGEL`)؛ التحكّمُ الأقوى شرطيٌّ بمستوى GRH (`residue → spectral` = GRH-level) — **يُسجَّل كشهادةٍ ناقصةٍ لا كتقدّم.**
- قربُ `WALL-POSITIVITY-WEIL`: `L(s,χ)` تلامس جدارَ الأصفار؛ لا يُعبَر.

## Opening boundaries (نافذة)

```text
- no raw text
- no full book import
- no RH/GRH progress
- no PNT/AP theorem claim
- no new character theorem
- no proof-system overclaim
- no classification-system change
- characters are treated as observables / residue-fiber tools, not as new results
- no unit writing yet
- no reading execution yet
```

**Honest classification:** Diagnostic (scope, planned). No RH/GRH progress.
