# IK-006-B — Large-value / Mean-value Machinery as Diagnostic Support

**Registry ID:** IK-006-B
**Status:** Ingested (v0.4)
**Classification:** **Diagnostic / Boundary** (Known underlying estimates read as diagnostic support; not results)

**Source Coverage:** Iwaniec–Kowalski, *Analytic Number Theory*, large-value estimates for Dirichlet polynomials and mean-value (second-moment) theorems (as recorded in transformed notes). **Transformed notes only — no raw text, no copied passages, no general book summary.** Book ID `BOOK-ANT-IWANIEC-KOWALSKI-003`. Local PDF outside git.

## Object

تقديراتُ القيم الكبيرة لمتعدّدات Dirichlet (`Σ a_n n^{-it}`) ومبرهناتُ المتوسّط (اللحظةُ الثانية `∫|Σ|^2`): كم مرّةً، وعند كم نقطةٍ متباعدة، يكون المجموعُ كبيرًا.

## Classical role

مبرهناتُ المتوسّط لمتعدّدات Dirichlet وتقديراتُ القيم الكبيرة **تُغذّي** تقديراتِ كثافة الأصفار `N(σ,T)` (IK-006-A). **مبرهناتٌ كلاسيكيّةٌ معروفة، مُستشهَدة.** متباينةُ الغربال الكبير هي **متباينةُ المتوسّط المحيطة** — تُذكَر كسياقٍ، **لا تُدخَل كوحدةٍ مستقلّة هنا.**

## Large-value diagnostic role

تُشخِّص كم قيمةً كبيرةً ممكنة → تحدُّ `N(σ,T)` → تحكّمٌ شرطيّ/متوسّطٌ في الخطأ. `TOOL-LARGE-VALUE-DIAGNOSTIC-001` — أداةُ دعمٍ تشخيصيّة تُغذّي كثافةَ الأصفار.

## Family / region / counting interpretation

```text
Family   : نقاطُ t حيث متعدّدُ Dirichlet كبير
Region   : نقاطٌ متباعدةٌ well-spaced في [−T, T]
Counting : العددُ R لهذه النقاط وحدُّه العلويّ (via mean value)
```

## PVG–ANT connection

مرصدُ اللحظة الثانية/اللاقطريّ الذي يُغذّي الكثافة: يتّصل بـ`WALL-OFF-DIAGONAL` و`WALL-DENSITY-HYP`؛ عددُ القيم الكبيرة = مقياسٌ يحدُّ بُعدَنا عن RH لاشرطيًّا.

## What it can diagnose

- التحكّمُ في اللحظة الثانية/المتوسّط دون RH.
- كيف يحدُّ عدُّ القيم الكبيرة كثافةَ الأصفار.
- موضعُ جدار فرضيّة الكثافة.

## What it cannot prove

- **لا يبرهن RH/GRH** — دعمٌ شرطيّ/متوسّطٌ لا برهان.
- لا مبرهنةَ قيمٍ كبيرةٍ جديدة، ولا تحسينَ كثافةٍ يُدّعى.
- لا يحلّ `MC-002` ولا `MC-005`.

## Related walls

`WALL-DENSITY-HYP` · `WALL-ZERO-FREE` · `WALL-POSITIVITY-WEIL` · `WALL-OFF-DIAGONAL`. **لا يُعبَر أيٌّ منها** (بما فيها `WALL-SIEGEL`).

## Related missing certificates

`MC-002` (خطأٌ قويٌّ لـψ(x)−x / RH) · `MC-005` (توزيعُ AP GRH-level) — **كلاهما شهادةٌ ناقصةٌ لا نتيجة، تبقى غيرَ محلولة.**

## Claim classification

**Diagnostic / Boundary.** الأساسُ Known ومُستشهَد؛ المخرجُ تشخيصٌ ودعمٌ شرطيّ، لا New Theorem ولا Candidate Mechanism.

## No-Go notes

```text
- large-value / mean-value machinery is diagnostic/conditional support feeding zero-density.
- the large-sieve inequality is ambient context, NOT a separate unit here.
- not RH/GRH progress; not a large-values theorem; not a zero-density/PNT/AP improvement; not a new theorem.
- no crossing WALL-SIEGEL or WALL-POSITIVITY-WEIL.
- MC-002 and MC-005 stay unsolved.
- no raw copyrighted text; transformed notes only.
```

## Next valid action

candidate v0.4 closure review (IK-006-A/B), then a further IK unit or another book — on explicit permission. **No IK-006-C, no large sieve as a separate unit, no expansion before review.**

## Audit checklist

- [x] Registry ID present · classification present (Diagnostic / Boundary)
- [x] Transformed notes only (no raw text, no copied passages, no general summary)
- [x] No RH/GRH progress · no large-values theorem · no zero-density improvement · no PNT/AP improvement · no new theorem
- [x] Large sieve not started as a separate unit · no IK-006-C
- [x] No crossing WALL-SIEGEL / WALL-POSITIVITY-WEIL · MC-002 & MC-005 unsolved
- [x] Six guards PASS after this unit

**Ceiling:** large-value / mean-value machinery is diagnostic and conditional support feeding zero-density — not RH/GRH progress, not PNT/AP improvements, not new theorems unless a full proof certificate exists. No RH/GRH progress.
