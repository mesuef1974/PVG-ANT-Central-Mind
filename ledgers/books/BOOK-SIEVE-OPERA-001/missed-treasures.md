# Opera de Cribro — Missed Treasures (honesty ledger)

Book ID: `BOOK-SIEVE-OPERA-001` · Version: v0.7 · **partial_overlay — `OPERA-004-A` CLOSED (v0.7-A PASS) + `OPERA-004-B` CLOSED (v0.7-B PASS).**

The honesty ledger of what was NOT extracted (yet) and why. No load-bearing node (`LB-01..06`) may be closed while deferred here; book-overlay closure can never be reached by silently deferring load-bearing material.

## Deferred within Chapter 1 (packet 001 range)

```text
- Full worked constants of the Eratosthenes–Legendre error accounting — read qualitatively only
  (aggregate order established; exact bookkeeping deferred). Reason: intake scope, not needed to state
  the certificate law.
- The individual chapter examples (m^2+1, m(m+2), p-2, Goldbach forms) as separate worked sequences —
  recorded as ONE fenced example set (TREASURE-OPERA-006), not mined per example. Reason: they are
  formulations, not certificates; per-example mining would inflate the ledger without new tools.
```

## Deferred beyond packet 001 (later units / packets)

```text
- Brun and Selberg sieve mechanics (upper/lower-bound sieves), the large sieve, beta-sieve, linear
  sieve — expected in later packets; provisional class reference_integration, to be confirmed per packet.
- The asymptotic sieve for primes and the bilinear input (Ch 16-18 band) — the core certificate-theory
  heart (LB-01, LB-02, LB-03); NOT reached by A; requires its own packets.
- Levels of distribution and special-sequence applications (Ch 19-22) — application_specific_certificate
  candidates requiring the full C_sieve record; not started.
```

## Boundary / registration status (v0.7-A adjudicated)

```text
- The three boundaries (aggregate-remainder · local-model · formulation-vs-certificate) were adjudicated
  by the v0.7-A Closure Review: all stay Boundaries, ZERO new WALL-IDs, and no existing wall is widened.
  aggregate-remainder is NOT mapped to WALL-SIEVE-CEILING (its registered text is about the RH ceiling,
  not aggregate remainder failure). formulation-vs-certificate is NOT mapped to WALL-PARITY — that
  re-examination is DEFERRED to the packet that source-grounds the parity phenomenon (a named, visible
  deferral, not a silent one).
- The model-subtraction diagnostic (TREASURE-OPERA-007) is a provisional-intake card, not yet a TOOL-ID;
  it is subject to duplicate_concept_audit and returns in the Chapter 18 material.
```

## OPERA-004-B deferrals (v0.7-B)

```text
- Selberg's sieve (quadratic-weight architecture) — DEFERRED, not rejected. B builds LB-03 from Brun's
  pure sieve + Fundamental Lemma; comparing Brun's combinatorial/truncation architecture with Selberg's
  optimized quadratic weights is a distinct unit, justified only if the COMPARISON itself is the treasure.
- Chapter 6 beyond §§6.1/6.5 (the rest of the combinatorial-sieve machinery) — not mined in this packet.
- The target-purity certificate (TREASURE-OPERA-013) is a NAMED certificate GAP, not a filled certificate;
  it stays a Boundary inside LB-03, and its relation to WALL-PARITY is deferred to the parity-grounding packet.
- model-subtraction (TREASURE-OPERA-007, from A) was NOT reused in Packet 002 — it remains an OPEN, named
  test debt: its live-function value is unproven until a unit actually computes c_n. Not closed by assertion.
```

**Ceiling:** every deferral above is a named, auditable item — none is a load-bearing node closed by silence. No RH progress. No GRH progress. No parity crossing. MC-001 unsolved.

**Honest classification:** Governance / Coherence (missed-treasures ledger, intake). No RH progress. No GRH progress.
