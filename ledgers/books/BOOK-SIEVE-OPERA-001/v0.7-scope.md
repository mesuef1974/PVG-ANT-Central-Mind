# v0.7 Scope — Opera de Cribro (Sieve / Parity / Distribution Certificate Theory)

Book ID: `BOOK-SIEVE-OPERA-001` · Version: v0.7 · **Opening Pass / scope-only: no reading execution, no unit writing, no trusted chapter classification yet.**

## Source (bibliographic identity — verified)

```text
John Friedlander, Henryk Iwaniec, Opera de Cribro.
American Mathematical Society, Colloquium Publications, vol. 57 (2010), ~554 pp.
Bibliographic identity verified (Sufyan, 2026-07-10).
Local source present outside git in Books_others/; PDF excluded from repository by .gitignore.
Ledger will contain transformed notes only, no book text.
```

The book's own introduction frames our central wall in its own words: the classical sieve cannot by itself produce primes because of the **parity phenomenon**; the bilinear input in the asymptotic sieve for primes is what allows crossing that barrier **in special sequences, not as a general solution**. This makes Opera a source witness on `WALL-PARITY` and on the corrected `MC-001` framing, not a promise to cross it.

## Declared objective (frozen)

```text
Extract the source-grounded sieve / parity / distribution certificate theory needed by the
PVG-ANT Central Mind. NOT mastery of sieve theory, and NOT an MC-001 solution program.
```

The reading is **book-first, not MC-001-first**: the objective is the certificate theory itself (what the sieve gives alone; what external input completes the deficit; when the bilinear input suffices; why it does not generalize), a lens wider than `MC-001` that contains it.

## Discovery / promotion / routing rules

```text
Discovery : source-first. MC-001 and project frontiers do NOT determine mining selection.
Promotion : project-governed, only after normalization. Frontier / MC-001 links go into
            integration-links.md AFTER the card is mined — never into the selection criterion.
Routing   : TOC/preface -> provisional routing hypothesis (recorded below).
            source packet -> grounded classification. closure review -> trusted status.
            A packet that overturns a hypothesis is recorded (packet_mismatch), never silently rewritten.
```

Governance for amendments, debts, the overlay/objective split, and the `C_sieve` record:
`governance/scope-amendment-and-debt-policy.md` (`SCOPE-AMENDMENT-AND-DEBT-POLICY-001`).

## Load-bearing certificate nodes (declared and FROZEN before mining)

No `book_overlay_closed` may stand while any node below (`load_bearing_for_declared_scope = true`) remains `capacity_deferred`. A source-boundary deferral is a named persistent debt (may still permit closure); a capacity deferral blocks full closure. Reduction requires proof of non-necessity; a newly discovered indispensable node requires a `load_bearing_discovery_amendment`.

```text
LB-01  generation of the parity obstruction                         status=unexamined  deferral_allowed=false
LB-02  emergence and role of bilinear remainders                    status=unexamined  deferral_allowed=false
LB-03  upper/lower-bound sieve versus asymptotic prime sieve        status=unexamined  deferral_allowed=false
LB-04  level of distribution and its exact function                 status=unexamined  deferral_allowed=false
LB-05  external analytic inputs and where they enter                status=unexamined  deferral_allowed=false
LB-06  application-specific success and failure of transfer         status=unexamined  deferral_allowed=false
```

## Unit classes (assigned from packets, never from the TOC)

```text
1. reference_integration          - known machinery re-encountered; its work is terminology
                                    reconciliation (Harman / Montgomery C,D language unification)
                                    and integration cards pointing at existing TOOL-IDs, NOT new tools.
2. core_certificate_theory        - the certificate-theory heart (parity obstruction, bilinear
                                    remainders, sieve-type distinctions, level of distribution).
3. application_specific_certificate - a special-sequence crossing; MUST carry a complete C_sieve
                                    record (governance policy §6). A success here does not transfer.
4. deferred_no_skill              - material that does not become a live skill for this mind;
                                    recorded explicitly in missed-treasures.md, never silently dropped.
```

## Provisional routing hypotheses (from TOC/preface — NOT trusted classification)

Recorded so a later packet can confirm, split, or overturn each one; the packet, not this table, classifies.

| Chapter / band (provisional) | Provisional hypothesis | Status |
|---|---|---|
| Early: inclusion-exclusion, Brun, Selberg | `reference_integration` (duplicate-pressure vs Harman / Montgomery D) | provisional |
| Large sieve, Bombieri-Vinogradov | `reference_integration` (duplicate-pressure vs Montgomery C / Harman) | provisional |
| Ch 16 Asymptotic Sieve and the Parity Principle | `core_certificate_theory` (diagnosis of the deficit; LB-01, LB-03) | provisional |
| Ch 17 combinatorial identities | `core_certificate_theory` (linear / bilinear forms; LB-02) | provisional |
| Ch 18 asymptotic sieve for primes | `core_certificate_theory` (the bilinear input; LB-02, LB-06) | provisional |
| Ch 19-21 special-sequence applications | `application_specific_certificate` (C_sieve required; LB-06) | provisional |
| Ch 22 levels of distribution | `core_certificate_theory` / `application_specific_certificate` (LB-04) | provisional |
| almost-primes on polynomials / algebraic forms | `application_specific_certificate` or `deferred_no_skill` | provisional |

Application samples (Ch 19-24 band) will be chosen **structurally**, not by prettiest success: a harmonic-input case, a spectral/automorphic case, a sparse-sequence case, an exceptional case (Linnik / exceptional character), and at least one **failure / unmet-condition** case — an atlas of non-transferable certificates, not a results gallery.

## Walls in play (uncrossed)

`WALL-PARITY` · `WALL-SIEVE-CEILING` · `WALL-SIEGEL` · `WALL-POSITIVITY-WEIL` · `WALL-ZERO-FREE`.

## Opening boundaries (in force)

```text
- no raw text
- no full book import
- no copied passages
- no mining, no unit written (0 OPERA-004-* files), planned.jsonl empty
- no trusted chapter classification before a Treasure Packet
- no MC-001-first selective reading
- no change to the Montgomery MNT-II book_overlay_closed state
- no RH/GRH progress, no parity-barrier break claim, no MC-001 solution path
- no mastery claim
```

## Next allowed action

```text
Await the first authorized Opera de Cribro Treasure Packet from Sufyan (via ChatGPT, the treasure analyst).
Only then: register the planned unit in planned.jsonl, classify it from the packet, and write ONE
OPERA-004-* unit at a time, followed by an adversarial closure review. Unit prefix: OPERA-004
(sieve layer, FRONTIER-ANT-PVG-004, matching the Harman pillar).
```

**Ceiling:** a special-sequence parity-crossing certificate is not a general crossing of the parity barrier, not an `MC-001` solution path, and not RH/GRH progress. Sieves count almost-primes; the parity barrier is uncrossed.
**Honest classification:** Governance / Scope Control (scope-only, planned). No RH progress. No GRH progress.
