# Next Action

```text
Current baseline: Opera de Cribro FIRST INTAKE, intake-corrected (v0.7, 2026-07-11).
                  OPERA-004-A (Sifting Sequences as Main-Term/Remainder Certificates) from
                  OPERA-TREASURE-PACKET-001 (Preface + Ch 1 §§1.1-1.4) = validated_intake — NOT closed.
                  Routing corrected: packet_mismatch = TRUE, mismatch_type = routing_split
                  (frozen hypothesis primary = reference_integration; packet grounded primary =
                  core_certificate_theory, secondary = reference_integration). books.jsonl = partial_overlay.
                  No new TOOL-ID; zero new WALL-ID (three boundaries stay Boundaries; aggregate-remainder
                  NOT mapped to WALL-SIEVE-CEILING; formulation-vs-certificate NOT mapped to WALL-PARITY,
                  parity re-examination deferred to the parity-grounding packet).

                  Montgomery MNT-II remains BOOK OVERLAY CLOSED (AUDIT-CM-MONTGOMERY-OVERLAY-008,
                  mastery deferred), untouched. Six trusted units C/D/E/F/G/H; quarantined
                  A/B/legacy-E (source-mismatch) unchanged.
                  Ceiling: sieve formulation is not prime production; no parity crossing; MC-001..005
                  unsolved; walls uncrossed; zero RH progress; zero GRH progress.

Review status:
  v0.7-A Closure Review — Attempt 1 = RETURN to validated_intake
    blocking_issue = packet_mismatch was recorded false; corrected to true / routing_split.
    additional_correction = over-early parity-wall mapping avoided (formulation NOT mapped to WALL-PARITY).
  Intake correction committed. This was an intake correction, NOT a State-Repair (no closed unit,
  no trusted claim altered).

Next action:
  v0.7-A Closure Review — Attempt 2 (re-run): deterministic audit + adversarial matrix T0-T6 +
  independent falsifier; if PASS: closure report + flip layers + one commit; if blocked: NO commit,
  report the finding only.

Not allowed (this phase):
  - OPERA-004-B, a second packet, or reading Chapter 2.
  - a new TOOL-ID, or any new WALL-ID (the three boundaries stay Boundaries).
  - mapping formulation-vs-certificate to WALL-PARITY before the parity-grounding packet.
  - putting MC-001 into the mining-selection criterion; links stay post-normalization.
  - MNTII-006-I or any Montgomery change; re-trusting quarantined A / B / legacy-E.
  - a new book beyond Opera; overlay closure of the book; flipping declared_objective_coverage.complete.

State:
  planned.jsonl = OPERA-004-A (validated_intake, not closed; packet_mismatch=true routing_split).
  Opera de Cribro (BOOK-SIEVE-OPERA-001) = partial_overlay (v0.7); unit OPERA-004-A intake;
    unit prefix OPERA-004; LB-01..06 frozen; governance/scope-amendment-and-debt-policy.md active.
  Montgomery MNT-II = book_overlay_closed (AUDIT-CM-MONTGOMERY-OVERLAY-008), Level 2,
    source-grounding-corrected, mastery deferred; trusted units C, D, E, F, G, H;
    quarantined A, B, E-legacy (source-mismatch / unvalidated).
  Guards (7): honesty · registry_sync · no_pdf · forbidden_promotion · duplicate_concept · citation · state_coherence.
```

**Honest classification:** Diagnostic (plan). No RH progress. No GRH progress.
