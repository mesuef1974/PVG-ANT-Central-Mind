# Current-Mind Freeze Manifest 001

**Manifest ID:** `CURRENT-MIND-FREEZE-MANIFEST-001`
**Program:** `PVG-ANT-RESEARCH-MODEL-PROGRAM-001` (stage S0 output)
**Recorded:** 2026-07-12
**Classification:** Diagnostic (comparison-arm definition; creates no theorem)

## Purpose

Define and freeze the composition of `ARM-CURRENT` — the full current mind
— so that every later capability claim by a trained local component is
measured against the strongest honest opponent, not a strawman.

## ARM-CURRENT composition

1. **Frontier engine:** Claude (model id `claude-fable-5`, knowledge cutoff
   2026-01), operating through the Claude Code harness with this
   repository's skills and guards available.
2. **Canonical repository:** `origin/main` of PVG-ANT-Central-Mind — the
   legal truth. Authoring-time snapshot: `2a6f0ff3a45030bd0526fa46147c52ea9c510c84`
   (P8 HOLD_AUTHORIZED merge, 2026-07-12).
3. **Memory layers:** `transition-memory/` (latest-state, next-action),
   `registries/` (158 ids at authoring: goals, rules, tools, books,
   maturation events), `maps/` (common-language contract v1, current
   capabilities), `ledgers/books/` overlays, `governance/` policies.
4. **Translation and ontology inventory at authoring:** 8 closed Language
   Kernel v1 families; 44 translation cards (24 Pass-001 + 12 Pass-002 +
   kernel bridges); 20 canonical native objects; 24 morphisms/projections;
   16 reconstruction/loss witnesses; loss levels LOSS-0..LOSS-4;
   maturation receipts = 7 (current: MATURATION-RECEIPT-007).
5. **Computational tools:** the repository guard and regeneration suite
   under `tools/` (11 core guards + deterministic example generators),
   plus the installed mathematical skills (analytic number theory,
   rigorous number theory, certificate ledger, conjecture guard,
   exact counts and zero tables, Lean formal bridge, LMFDB/OEIS lookup).

## Freeze rule (binding)

- For every benchmark or comparison run, `ARM-CURRENT` = the frontier
  engine + this repository at the exact commit SHA recorded in that run's
  pre-run receipt, with tools, memory, system prompt, and attempt count
  frozen at gate G1 sealing.
- No modification of any layer during a run.
- Any change after sealing (engine version, repository content, tool set)
  produces a **new arm version** and must be recorded in the run receipt
  with contamination accounting — it is never silently absorbed.
- The frontier engine's exact model id is re-recorded in every run receipt;
  if it differs from this manifest, the deviation is named in the receipt.

## Constraints inherited from the benchmark protocol

- `ARM-CURRENT` never sees Hidden Set B or any gold keys before the
  custodian opens them at S8.
- The answering role is separated from case authorship, key verification,
  and scoring, per `governance/programs/ADVERSARIAL-PVG-ANT-BENCHMARK-002.md`.
- Registry coverage scores (e.g. Benchmark 001: 702/720 post-Pass-002) are
  coverage measurements, not autonomous performance; hidden-set autonomous
  performance = not measured until S2.

## Ceiling

Freezing a comparison arm certifies nothing about its capability. No
theorem, no originality certificate, zero RH progress, zero GRH progress.
